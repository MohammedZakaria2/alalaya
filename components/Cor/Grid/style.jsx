import styled from 'styled-components';
import { colors } from '../../theme';

export const Wrapper = styled.div`
  margin: 1rem 1.2rem 6rem 1.2rem;
  background-color: ${colors.white};
  padding: 2.5rem 2rem;
`;
